# Windows real-model stem-separation validation

Recovery checkpoint for issue #32. The branch will contain a manual-only Windows x86-64 CPU workflow, a real FastAPI validator using deterministic synthetic audio, permanent offline/static tests, and safe workflow evidence. No model, cache, runtime, generated audio, stems, database, or machine path is committed.
