# Local stem-separation validation

## Recovery status

Issue #34 implementation is being recovered on `agent/local-separation-doctor`.

This initial remote checkpoint exists only to open the required draft pull request before publishing the completed doctor, tests, platform wrappers, and final guide. It makes no validation claim.

The passive doctor mode must remain model-free and must not alter existing PopEx jobs, exports, installed runtimes, or model caches.
