# Direct runtime inputs to evaluate for Windows x86-64 / CPython 3.13.
# Resolve ordinary packages only from https://pypi.org/simple.
# Resolve torch separately from https://download.pytorch.org/whl/cpu.
# This file is an investigation input until the Windows workflow validates the full lock.

demucs==4.1.0
numpy==2.5.1
huggingface-hub==1.26.0
safetensors==0.8.0
PyYAML==6.0.3
sphn==0.2.1
julius==0.2.8
lameenc==1.8.4
einops==0.8.2
tqdm==4.70.0

# Evaluated separately from the official CPU index:
# torch==2.13.0+cpu

# Installed last from the checked-out local source tree after the wheel lock is complete:
# popex-demucs-worker==1.0.0 from runtimes/demucs_worker

# Deliberately excluded: torchaudio, openunmix, dora-search,
# demucs[train], CUDA, and NVIDIA packages.
