# Direct runtime inputs for windows-x86_64-cpu-cpython313.
# Resolve ordinary packages only from https://pypi.org/simple.
# Install torch separately from https://download.pytorch.org/whl/cpu.

demucs==4.1.0
numpy==2.5.1
huggingface-hub==1.26.0
safetensors==0.8.0
PyYAML==6.0.3
sphn==0.2.1
julius==0.2.8
lameenc==1.8.4
einops==0.8.2
tqdm==4.70.0

# Installed separately from the official CPU index:
# torch==2.13.0+cpu

# Installed last from the local checked-out source tree with --no-deps:
# popex-demucs-worker==1.0.0 from runtimes/demucs_worker

# Deliberately excluded: torchaudio, openunmix==1.3.0,
# dora-search==0.1.12, demucs[train], CUDA, and NVIDIA packages.
